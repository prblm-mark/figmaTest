import { createIcons, icons } from 'lucide';

// Render all elements with a data-lucide="<icon-name>" attribute.
// In production builds, replace `icons` with specific named imports to tree-shake:
//   import { ArrowRight, ChevronDown, X } from 'lucide';
//   createIcons({ icons: { ArrowRight, ChevronDown, X } });
createIcons({ icons });
