/* ─── PARKED 2026-08-28 — NO FIGMA COMPONENT EXISTS ────────────────────────────────────
 *
 * Excluded from Code Connect by its `.parked` extension; publish is all-or-nothing.
 *
 * `4218-5304` fails as "node is not a top level component or component set", and there is no
 * AssistantPopover component in the CC Hybrid file (see the sixteen-entry inventory listed in
 * NotificationBadge.figma.ts.parked).
 *
 * Note the adjacency: `4218:4681` IS the IconNavigation set, so `4218-5304` is a sibling node
 * in the same neighbourhood of the canvas — consistent with the popover having been drawn
 * beside IconNavigation rather than published as a component. The nearest real candidate is the
 * AiAssistant set (`4152:8323`), whose `Type` includes a **`Pop Over`** option — that may be
 * where this design now lives, but repointing there would attach this markup to a different
 * component, so it was NOT done without the designer confirming.
 *
 * TO REINSTATE: confirm whether AssistantPopover is AiAssistant's `Type=Pop Over` variant. If
 * so, point at `4152:8323` with `variant: { Type: 'Pop Over' }`.
 */

import figma, { html } from '@figma/code-connect/html'

figma.connect(
  'https://www.figma.com/design/ETKqleZdpertwFEo40YB5n/Affino-CC-Hybrid--Design-System?node-id=4218-5304',
  {
    example: () => html`
      <div class="cc-assistant-popover">
        <div class="cc-assistant-popover__header">
          <div class="cc-assistant-popover__brand">
            <span class="cc-assistant-popover__badge">
              <i data-lucide="sparkles" aria-hidden="true"></i>
            </span>
            <p class="cc-assistant-popover__title">Affino Assistant</p>
          </div>
          <button class="btn btn--tertiary btn--sm btn--icon cc-assistant-popover__close" type="button" aria-label="Close">
            <i data-lucide="x" aria-hidden="true"></i>
          </button>
        </div>
        <div class="cc-assistant-popover__body">
          <div class="cc-assistant-popover__intro">
            <p>Affino’s Assistant is great at providing in context support whilst you work in the control center.</p>
            <p>Stuck on screen? Launch the Affino Assistant from the Actions Menu to get moving.</p>
          </div>
          <button class="btn btn--primary cc-assistant-popover__launch" type="button">
            Launch
            <i data-lucide="arrow-right" aria-hidden="true"></i>
          </button>
        </div>
      </div>
    `,
  }
)
