# Component Registry

| Component | Tier | Status | Figma URL | Notes |
|---|---|---|---|---|
| ActionCard | component | Built | [node 2930:5757](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2930-5757) | `src/components/ActionCard/` — single-row card, fluid ellipsis title + trailing action. 2 Action variants × Default/Hover (CSS `:hover` border darken). Action=Button composes Button (Tertiary, **xs**); Action=Right Chevron renders as `<a>` with `chevron-right`. Height `--ai-spacing-10` (56px). |
| AudioPlayer | component | Built | [node 3069:5884](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=3069-5884) | `src/components/AudioPlayer/` — article audio player. One unified waveform card (redesign supersedes the old `--compact`/`--card`/`--mini` Types). Type = Default / Playing / Sticky × Device (Desktop/Mobile via `@media (max-width:767px)`). CSS-grid reflow. Waveform is the sole seek control (thin scrubber removed); played bars turn `--ai-surface-brand`. `--sticky` = docked appearance (article title + `--ai-shadow-md`); JS `--dock` fixes it to the viewport bottom on scroll (IntersectionObserver, grouped/synced via `[data-ap-group]`). Play fill normalised to `--ai-surface-brand`. Play/pause icon-swap, click-seek deterministic waveform, remaining-time toggle; download is **backend TODO**. `AudioPlayer.js` auto-inits `[data-ap-player]`. |
| Button | component | Built | [node 53:2489](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=53-2489) | `src/components/Button/` — primary, secondary, tertiary, alert, alert-outline, icon-only variants. Sizes: base, sm, **xs** (xs added 2026-06-12: medium weight, `--ai-radius-sm`, `--ai-spacing-1` gap, 12px icons). Alert hover/pressed use Red/400+600 primitives (approved). |
| ButtonGroup | component | Built | [node 2440:945](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2440-945) | `src/components/ButtonGroup/` — 16 variants: Type (Standard/With Icons/Dropdown/Icon Only) × Size (Base/sm) × Color (Default/Brand) × Direction (Horizontal/Vertical). Composes Button. |
| InfoLabel | component | Built | [node 68:4425](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=68-4425) | `src/components/InfoLabel/` — No Label=False (text+icon), No Label=True (icon only) |
| Input | component | Built | [node 78:2016](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=78-2016) | `src/components/Input/` — Base + sm sizes, Default/Hover/Focus/Active/Error states. Optional label back button (`show label back`, scoped `.input__label-back`). **The `--sm` 192px `max-width` was removed 2026-08-25** — undocumented, sat on a *size* modifier, and had already been worked around in Datatables, ControlScreen and Unassigned before it blocked TableListing's 320px search. Removal proved safe by measuring every existing `input--sm` wrap before/after at two widths (identical); consumers wanting a narrow small input now set their own width. |
| DatePicker | component | Built | [node 3039:8761](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=3039-8761) | `src/components/DatePicker/` — functional vanilla-JS calendar. 7 Figma variants = modes/views of one picker: single, no-selection (today), disabled-past, range single/dual month, month + year drill-down selectors. Composes Input for the trigger field. `DatePicker.js` auto-inits `[data-datepicker]`. |
| FilterDropdownItem | component | Built | [node 3032:18152](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=3032-18152) | `src/components/FilterDropdownItem/` — selectable filter option row (title + optional sub text + green check when selected). 4 variants: Type (Initial/Selected) × State (Default/Hover). Multi-select toggle via `FilterDropdownItem.js` (`aria-pressed`, `filter-dropdown-item:toggle` event). |
| FilterDropdownItemGroup | component | Built | [node 3032:18397](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=3032-18397) | `src/components/FilterDropdownItemGroup/` — bordered card stacking FilterDropdownItem rows. 2 variants (Property 1): w/Subtext, Default. Composes FilterDropdownItem; no own JS (relies on FilterDropdownItem.js). |
| Tooltip | component | Built | [node 169:3025](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=169-3025) | `src/components/Tooltip/` — single variant, dark panel. Positioning owned by parent. |
| Pill | component | Built | [node 68:4503](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=68-4503) | `src/components/Pill/` — 5 variants: Default (invert), Contrast, Success, Warning (error), Brand. Uses semantic surface tokens. |
| Portraits | component | Built | [node 68:4517](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=68-4517) | `src/components/Portraits/` — Single `.portrait` class; fills container with `object-fit:cover`. Clipping is Avatar's responsibility. Figma has ten portrait symbols under the Portraits section frame: Female 1 `68:4785`, 2 `68:4782`, 3 `68:4783`, 4 `68:4779`, 5 `68:4777`; Male 1 `68:4784`, 2 `68:4781`, 3 `68:4780`, 4 `68:4778`, 5 `68:4776`. Code currently ships Female 1–5 and Male 1–2; **Male 3/4/5 are in Figma but not built.** (Previously this row pointed at the Female 1 symbol and claimed Female 2 + Male 1 were "not yet located" — both are present in the code and in Figma.) |
| Avatar | component | Built | [node 68:5042](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=68-5042) | `src/components/Avatar/` — **80 variants** (Figma-formalised 2026-05-07). Six axes: Size (1-5: 24-80px) × Shape (Round/Rounded) × Type (Default/Bordered/Placeholder/Initials) × Show Notification (True/False) × Notification Color (Green/Red/Orange) × Checked (True/False). Composes Portraits + Lucide (`user`, `check`). Photo clipped via `clip-path` (circle for Round, inset-rounded for Rounded) so the notification dot at top-right stays visible. No JS. |
| Badge | component | Built | [node 2580:10544](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2580-10544) | `src/components/Badge/` — 6 Type × 5 status states + a State=Default reserved for the Size demo (sm/Default/lg). Type: Default / Border / Pill / Icon Left / Indicator / Dismissible. State: Default (Info-coloured), Neutral, Success, Warning, Danger, Info. JS auto-removes a `.badge` when its `.badge__close` button is clicked (event-delegated). |
| Breadcrumb | component | Built | [node 2580:11309](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2580-11309) | `src/components/Breadcrumb/` — 4 Type variants: Default, Home Icon, Solid Background, Dropdown. Last item = current page (`aria-current="page"`); chevron-right separators use `--ai-icon-contrast`, home icon uses `--ai-icon-secondary`. Type=Dropdown composes the Dropdown component for trigger/panel behaviour. Note: Figma frame mislabelled "Numbered List" — flagged for designer rename. |
| Radio | component | Built | [node 2030:1622](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2030-1622) | `src/components/Radio/` — Initial, Checked (4px brand border ring), Disabled. Native `<input type="radio">`, single-select via `name` attribute. |
| Checkbox | component | Built | [node 2043:2985](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2043-2985) | `src/components/Checkbox/` — Initial, Checked (brand bg + check icon), Disabled. Native `<input type="checkbox">`, radius-sm. |
| ToggleDS | component | Built | [node 2025:1080](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2025-1080) | `src/components/ToggleDS/` — Design System toggle (3 sizes: SM/Default/LG × 3 states: Initial/Active/Disabled). Distinct from AI Chat Toggle. |
| FileInput | component | Built | [node 2043:3128](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2043-3128) | `src/components/FileInput/` — 2 sizes (Default/Small) × 2 types (Default/Brand) × 3 states (Initial/Selected/Error). Native file picker, filename display, clear button. |
| DragDropFile | component | Built | [node 2043:3377](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2043-3377) | `src/components/DragDropFile/` — 4 variants: Default, Active (drag-over), With Button (browse), Error. Dashed border zone, drag-and-drop JS. Composes Button. |
| Textarea | component | Built | [node 2458:433](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2458-433) | `src/components/Textarea/` — 8 variants: Type (Default/Character Count/Error/Disabled) × Size (Base/sm). Token-aligned with Input for form consistency. |
| PromptTemplateItem | component | Built | [node 2:7446](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-7446) | `src/components/PromptTemplateItem/` — Default, Hover, Selected (radio toggle), Expanded (chevron-triggered, independent of selection). Custom icon-per-item via Lucide. |
| Modal | pattern | Built | [node 2464:763](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2464-763) | `src/patterns/Modal/` — 6 DS variants: Default, Small, Large, Confirmation, Form, Scrollable. Composes Button, Input, Textarea, Checkbox. **Responsive (2026-07-28):** below `--ai-bp-sm` (639px) `.modal__header` / `.modal__footer` padding tightens to `--ai-spacing-4 --ai-spacing-5`; `.modal--confirm`'s bespoke footer padding and `.modal__body` are unaffected. Figma has no mobile modal variant. |
| FilterDropdowns | pattern | Built | [node 3039:5639](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=3039-5639) | `src/patterns/FilterDropdowns/` — filter popover bodies (320px card + Apply). **Core phase built:** Text, Checkbox, Multi Select (±search), Select Options (±subtext — the two Select versions: field + options group, no Apply), Date In the last/Range/Equal To, More Filters. (Plain Select + w/Placeholder consolidated into the two Select Options versions per design direction.) Composes Input, Checkbox, FilterDropdownItem(Group), FilterItem, DatePicker, Datatables, Modal, Button. **Phase 2:** Predictive Text Options (±subtext, type-to-reveal single-select), Multi Select Table (640px), Multi Select Modal (960px). |
| FeedbackModal | pattern | Built | [node 27:33615](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=27-33615) | `src/patterns/Modal/` — 2 AI Chat variants: Feedback Positive, Positive Negative. Uses --ai-chat-brand for buttons/checkboxes (accent colour). Moved from DS to AI Chat file. |
| Header | pattern | Built | [node 2:6854](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-6854) | `src/patterns/Header/` — State (Default/Tooltip/Discard) x Device (Default/Mobile). Composes Tooltip, InfoLabel, Button. |
| VersionHistoryRow | pattern | Built | [node 2:7693](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-7693) | `src/patterns/VersionHistoryRow/` — Default, Live, Selected, Selected & Live variants. Composes Avatar, Pill. |
| VersionHistory | pattern | Built | [node 2:7756](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-7756) | `src/patterns/VersionHistory/` — 2 variants: Default (collapsed, 5 rows) + Expanded (12 rows). Custom heading with `history` icon + `chevron-right` toggle. Composes VersionHistoryRow, Avatar, Pill. No Header/Button dependency. |
| PromptTemplates | pattern | Built | [node 2:7662](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-7662) | `src/patterns/PromptTemplates/` — single variant panel: heading + list of PromptTemplateItem rows. Composes PromptTemplateItem. |
| SystemRole | template | Built | [node 2:9344](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-9344) | `src/templates/SystemRole/` — Default (full modal, blurred overlay) + Minimised (400px panel, draggable, left-edge resizable) + Mobile variant (176:3242): full-viewport CSS-only layout at <=767px, no resize/drag. Uses `--ai-surface-elevated-1` bg + `--ai-border-secondary` border. Composes Header, VersionHistory, PromptTemplates, Button, InfoLabel. |
| Skeleton | component | Built | [node 2077:1527](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2077-1527) | `src/components/Skeleton/` — CSS-only shimmer animation, 5 lines of varying widths. Uses `--ai-skeleton-base` + `--ai-skeleton-highlight`. |
| WorkingIntro | component | Built | [node 2077:1491](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2077-1491) | `src/components/WorkingIntro/` — 3 stages: logo pulse, title SplitText reveal, subtitle ScrambleText. GSAP SplitText + ScrambleTextPlugin. |
| SourcesCarousel | pattern | Built | [node 2077:1588](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2077-1588) | `src/patterns/SourcesCarousel/` — Horizontal card carousel with scroll snap, GSAP entrance stagger, arrow nav. Composes Button. |
| ChatResponse | template | Built | [node 2089:6577](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2089-6577) | `src/templates/ChatResponse/` — 8s master GSAP timeline. Composes WorkingIntro, SourcesCarousel, Skeleton. |
| ChatSidebarItem | component | Built | [node 2:1668](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-1668) | `src/components/ChatSidebarItem/` — Type=Thread (Default, Hover, Menu, Selected + Pinned) + Type=Action (icon+label, Default/Hover/Selected). Computed overlay bg via `color-mix()` + `sidebar-colors.js`. Composes ChatSidebarMenu. |
| ChatSidebarMenu | component | Built | [node 2:1668](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-1668) | `src/components/ChatSidebarMenu/` — Context menu dropdown for ChatSidebarItem. Save, Copy, Copy Link, Delete. Uses computed sidebar-theme colors. Shadow raw values (no shadow tokens yet). |
| ChatSidebar | pattern | Built | [node 2:2006](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-2006) | `src/patterns/ChatSidebar/` — Sidebar navigation panel with logo, action items, scrollable sections (Pinned/Recent/Older). Composes ChatSidebarItem, ChatSidebarMenu. |
| ChatHeader | pattern | Built | [node 2:2182](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-2182) | `src/patterns/ChatHeader/` — Header bar with assistant selector dropdown, admin toggle buttons, window controls. Type=Default/Minimised/Mobile, Options=Center/Align Left/No Border. Composes Button. |
| MessageBubble | component | Built | [node 2:1313](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-1313) | `src/components/MessageBubble/` — User question bubble, right-aligned. Default + Hover (copy/link action buttons). Uses `--ai-chat-msg-bg/text`. Composes Button. |
| MessageInput | pattern | Built | [node 2:617](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-617) | `src/patterns/MessageInput/` — Chat input bar with textarea, filter button + duration popover, send button, disclaimer. Desktop/Mobile/Minimised. Active state via JS. Composes Button, Tooltip. |
| ChatMain | pattern | Built | [node 2:3752](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-3752) | `src/patterns/ChatMain/` — Multi-view chat area: Initial (welcome + SuggestedQuestion cards), Processing (ChatResponse animation), Response (MessageBubble + prose + sticky input). GSAP-animated transitions. Composes SuggestedQuestion, MessageBubble, MessageInput, WorkingIntro, SourcesCarousel, Skeleton. |
| SuggestedQuestion | component | Built | [node 2:64](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-64) | `src/components/SuggestedQuestion/` — Clickable question card for initial chat view. Icon + text, `--ai-surface-contrast` bg (via `data-surface="chat"` context). Type=Default/Desktop (grid context determines size). |
| AiChat | template | Built | [node 2:9299](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-9299) | `src/templates/AiChat/` — Full-page AI chat template. Sidebar hidden on mobile, inline at >=1024px (lg). Toggle via panel-left button. Composes ChatSidebar, ChatHeader, ChatMain. |

| AiChatMinimised | template | Built | [node 2160:4810](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2160-4810) | `src/templates/AiChatMinimised/` — Floating popup chat widget. Fixed-size (384px), draggable via header, rounded corners, expand/close window buttons. Composes ChatHeader (--minimised), ChatMain. |
| SourcesLink | component | Built | [node 2:80](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-80) | `src/components/SourcesLink/` — Pill-shaped link. Type=Primary (brand-colored, dynamic via `color-mix()` + `data-brand-theme` luminance detection) + Type=Link (neutral). Composes nothing. |
| QuickLinks | pattern | Built | [node 2:571](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-571) | `src/patterns/QuickLinks/` — Heading + flex-wrap row of SourcesLink pills. Composes SourcesLink. `--full` modifier for Guides/FAQs (uncapped width). |
| ChatResponseTable | component | Built | [node 2:1434](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-1434) | `src/components/ChatResponseTable/` — Inline data table for chat responses. Default (mobile) + Desktop variants. Responsive font sizing at 640px. |

| Toggle | component | Built | [node 2025:1080](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2025-1080) | `src/components/Toggle/` — On/off switch. **Ships `Toggle.js` since 2026-08-26** — document-delegated in the CAPTURE phase (so consumers reading `.toggle--active` synchronously see the post-click state, and dynamically-injected toggles work), flips the class + `aria-checked` and emits `toggle:change` (detail `{ active }`). It was CSS-only before that, so the switch did not actually work and **nine consumers had each re-implemented the same three-line flip inline** — all removed; AiChat's two handlers, which also did side effects, were converted to `toggle:change` listeners. State=Initial/Active/Disabled × Size=**xxs**/xs/SM/Default/LG (14 variants). **`toggle--xxs` added 2026-08-25** for TableListing — a real Figma variant the component was missing (24×12 track, 8×8 knob, all tokens); Figma defines only Initial + Active for that size, no Disabled. Default Active track = `--ai-surface-brand` (was `--ai-chat-brand` until 2026-05-27 — switched to align with new Dropdown user-menu spec; StyleSettings retains chat-brand via contextual override). 44px/28px/20px track + thumb dims kept as raw px (sub-token optical). |
| SegmentedControl | component | Built | [node 2162:5079](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2162-5079) | `src/components/SegmentedControl/` — Radio-style toggle. State=Light/Dark. Active bg `--ai-surface-minimal`, active radius `--ai-radius-sm`. Lucide icons. |
| ColorPickerInput | component | Built | [node 2:6620](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-6620) | `src/components/ColorPickerInput/` — Color swatch + hex display. Native `<input type="color">` picker. |
| StyleSettings | pattern | Built | [node 2:6628](https://www.figma.com/design/Ikv8jxb5dcRH8ff4q4dR11/Affino-AI---AI-Chat?node-id=2-6628) | `src/patterns/StyleSettings/` — Modal settings panel. Uses `--ai-surface-elevated-1` bg. Theme toggle, color pickers, font/layout dropdowns, border/question toggles. Composes Toggle, SegmentedControl, ColorPickerInput, Button, Tooltip. |
| Accordion | component | Built | [node 2527:2622](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2527-2622) | `src/components/Accordion/` — Collapsible item list. Type=Default/Separated Cards/All Open/Flush/Nested × Size=Default/sm. Single- or multi-open via `data-acc`. Lucide chevron-down. |
| TimePicker | component | Built | [node 2522:1532](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2522-1532) | `src/components/TimePicker/` — Six time-entry compositions: Single input, Duration, Timezone, Range, Range Reveal, Slots. Self-contained. Lucide clock/chevron-down/globe/plus. |
| RangeSlider | component | Built | [node 2527:1876](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2527-1876) | `src/components/RangeSlider/` — Native `<input type="range">` with invisible native thumb + visible DOM thumb (JS-positioned for Figma fidelity). Type=Counter/Default/Disabled/Steps/Labels × Size=Default/sm/lg. Brand-fill gradient painted via JS. |
| Select | component | Built | [node 2527:1995](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2527-1995) | `src/components/Select/` — Button-based triggers (no native `<select>`) so values capture cleanly. Type=Default/Multiselect/Disabled/Category Dropdown/Underline × Size=Default/sm. |
| SearchInput | component | Built | [node 2522:711](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2522-711) | `src/components/SearchInput/` — 11 variants across Basic, Basic Icon Only, Category Dropdown Search, Split, Split Icon Only, Locations Search, Voice Search, Advanced × Default/sm sizes. Three structural patterns: single field, split field+button, segmented group. |
| Chart | pattern | Built | [node 2527:2215](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2527-2215) | `src/patterns/Chart/` — Card chrome (header/canvas/footer) in tokens; visualisation via Chart.js v4.4.0 CDN. Type=Multiple Lines/Single Line/Bar Chart/Doughnut. Brand colours pulled from `--ai-*` at runtime. Only design-system pattern using an external charting lib. |
| Banner | pattern | Built | [node 2550:2225](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2550-2225) | `src/patterns/Banner/` — 12 variants: Type (Announcement/Marketing/Information) × Position (Floating/Fixed) × Device (Desktop/Mobile via @media). Composes Button. Marketing & Information mobile stack vertically with Sign Up full-width; Marketing drops Learn More on mobile. |
| Dropdown | component | Built | [node 2553:3676](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2553-3676) | `src/components/Dropdown/` — **7 variants** (added User menu + User Menu Icon Nav on 2026-05-27): Basic, With Header, Actions, Checkbox, Search, User menu, User Menu Icon Nav. Trigger reuses `.btn--secondary`; Delete CTA reuses `.btn--alert`; rows composed via **DropdownItem** (extraction); Checkbox variant composes Checkbox; User menu variants compose ThemeToggle + Toggle. JS handles toggle/outside/Escape, search filtering, and linked-toggle reveals (`data-reveal-by` on a row links it to a source `.toggle` — when the toggle becomes active, the row gains `.is-revealed`). Legacy `.dropdown__item` kept as back-compat alias. |
| DropdownItem | component | Built | [node 2699:2149](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2699-2149) | `src/components/DropdownItem/` — Single row in a Dropdown panel. State (Default/Hover) × Type (Default/Warning) = 4 variants. Default Warning is visually identical to Default Default; only Hover Warning shows the destructive red treatment. Extracted from Dropdown's inline `.dropdown__item` selectors on 2026-05-27 to match Figma's formal componentisation. |
| ThemeToggle | component | Built | [node 2699:2052](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2699-2052) | `src/components/ThemeToggle/` — Inline segmented Light \| Dark control. 2 variants (State=Light/Dark). Click flips `data-theme` on `<html>` and persists to localStorage (`'demo-theme'` key — shared with `dark-mode-toggle.js`). Distinct from the demo-page floating gear toolbar (`dark-mode-toggle.js`); ThemeToggle is the app-level inline equivalent used inside dropdowns / settings. |
| Table | component | Built | [node 2557:6203](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2557-6203) | `src/components/Table/` — 3 type variants: Default, Striped, Bordered. Native HTML table with `border-collapse: collapse`. Mobile is responsive — `.table-wrap` adds `overflow-x: auto` at ≤767px so columns scroll horizontally. No JS. Figma node 2557:6204 is mislabelled `Device=z`; visually equals Type=Border, Device=Mobile and is covered by the responsive CSS. |
| Datatables | component | Built | [node 2562:8289](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2562-8289) | `src/components/Datatables/` — **9 variants** (2026-05-29: added Whos Online Type axis): Type (Pagination/Search/Whos Online) × Device (Desktop/Mobile) × Overflow (Scroll/Trigger; Trigger is mobile-only). Composes Table + Button + Input + **Avatar + Portraits** (Whos Online USER cell). Whos Online toolbar combines page-size select + Search (no action buttons); body adds `.datatables__user-cell` (avatar + name/role stack), text-only tertiary Button CTA, and a non-sortable TOUCH column with Lucide `fingerprint`. Pure-CSS kebab-row-expand for Trigger overflow via `:has(:checked)`. No JS. Uses dedicated `--ai-datatable-*` token namespace (table-bg / table-header-bg / table-subheader-bg / table-footer-bg / table-expanded-bg / table-border) sourced from `components/global/datatable` in the Semantic mode files. |
| Spinner | component | Built (code-first) | [node 2619:2156](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2619-2156) — **Figma has since adopted this as a `Spinners` component set (Type × Tier × Size × State); the code has NOT been reconciled against it.** Originally pushed code-first as 2618:2. | `src/components/Spinner/` — Pure-CSS rotating ring (border + `color-mix()` for faded track + solid leading arc). 4 sizes (sm/default/lg/xl tied to `--ai-icon-size-*`) × 7 colours (`--brand` default, `--success`, `--warning`, `--danger`, `--info`, `--neutral`, `--inverted` for use on coloured surfaces). `role="status"` + visually-hidden label for SR. Honours `prefers-reduced-motion`. No JS. Flowbite-inspired; pushed to Figma for designer to formalise. |
| MainMenuItem (CC) | component | Built | [node 4057:2802](https://www.figma.com/design/ETKqleZdpertwFEo40YB5n/Affino-CC-Hybrid--Design-System?node-id=4057-2802) | `src/cc/components/MainMenuItem/` — Atomic row used inside the CC Menu (Control/Analysis layouts). Variants: State=Default/Hover/Selected/Expanded. Label stays `--ai-text-invert-secondary` across states; Selected/Expanded brighten label to `--ai-text-invert` and icon to `--cc-mainmenu-icon`. Hover/Expanded show a trailing `chevron-down`; Expanded rotates it 180°. Demo activates `data-brand="cc"`. |
| Sidebar (CC) | pattern | Built (visual only) | [node 4055:10681](https://www.figma.com/design/ETKqleZdpertwFEo40YB5n/Affino-CC-Hybrid--Design-System?node-id=4055-10681) | `src/cc/patterns/Sidebar/` — Narrow icon rail (56px Desktop, 52px Mobile). 5 variants across Device × {Default, Selected, Expanded(mobile only)}. Mobile pins an EllipsisVertical button at the bottom via `.cc-sidebar__spacer`. Brand cell uses CSS `mask` over `affinoLogo.svg`, themed by `--cc-mainmenu-icon`. Active button = `--cc-mainmenu-secondary-bg`. Demo activates `data-brand="cc"`. |
| Menu (CC) | pattern | Built (visual only) | [node 4061:16921](https://www.figma.com/design/ETKqleZdpertwFEo40YB5n/Affino-CC-Hybrid--Design-System?node-id=4061-16921) | `src/cc/patterns/Menu/` — 280px panel. 8 variants across Type (Control/Analysis/Favourites/CRM) × State (Default/Selected/Scrolled/Expanded). Control/Analysis/Favourites compose MainMenuItem rows; CRM uses a bespoke layout (`.cc-menu__md-btn` + Recent contacts + show more/less toggle). Affino wordmark via CSS `mask`. Search input is white-on-navy. Submenu actions (pin/trash) fade in on row hover. Demo activates `data-brand="cc"`. |
| Sidebar Menu (CC) | pattern | Built (visual only) | [node 4066:18917](https://www.figma.com/design/ETKqleZdpertwFEo40YB5n/Affino-CC-Hybrid--Design-System?node-id=4066-18917) | `src/cc/patterns/SidebarMenu/` — Pure layout composite: `display: inline-flex` row of Sidebar + Menu. 2 device variants (Desktop/Mobile). Composes the Sidebar pattern (rail) and the Menu pattern (panel) — see those rows for variant detail. **No JS yet** — submenu open/close, scroll-shadow, and keyboard nav are a follow-up. |
| NotificationBadge | component | Built | [node 4095:3413](https://www.figma.com/design/ETKqleZdpertwFEo40YB5n/Affino-CC-Hybrid--Design-System?node-id=4095-3413) | `src/components/NotificationBadge/` — Bell trigger + overlaid count badge. 2 sizes (default 40px / sm 32px) via `.btn--icon` and `.btn--icon.btn--sm`. Badge uses `--ai-surface-error` (Red 600) since Red 500 (Figma value) has no semantic token; 8px font and translate-based offset documented as optical exceptions. Composes Button. |
| Top Navigation (CC) | pattern | Built (visual only) | [node 4099:3634](https://www.figma.com/design/ETKqleZdpertwFEo40YB5n/Affino-CC-Hybrid--Design-System?node-id=4099-3634) | `src/cc/patterns/TopNavigation/` — Dark utility bar above the main Header. 4 variants (Type=Default/Multizone × Device=Desktop/Mobile) reduced to a single responsive structure via `@media (max-width: 767px)`. Multizone uses existing Dropdown component for "Zone Selector" trigger. Contextual override scoped to `.cc-top-navigation .breadcrumb__*` flips Breadcrumb colours to `--ai-text-invert-secondary/invert` (Case B per CLAUDE.md). User dropdown trigger is visual only (interaction deferred). Composes Breadcrumb, Dropdown, Portraits. |
| Header (CC) | pattern | Built (visual only) | [node 4105:3641](https://www.figma.com/design/ETKqleZdpertwFEo40YB5n/Affino-CC-Hybrid--Design-System?node-id=4105-3641) | `src/cc/patterns/Header/` — Main white page header. 6 variants (Type=Default/Sub Text/Control × Device=Desktop/Mobile) collapsed to one responsive structure. Mobile: title shrinks 22→16px, buttons in `.cc-header__actions` collapse text→icon-only 32px square via `.cc-header__btn-label` hide. Control variant shows avatar + greeting + user details on desktop; mobile hides "Hi " prefix and user details. Kebab wrapped as `.cc-header__kebab.dropdown__trigger`. Composes Button, Dropdown, NotificationBadge, Portraits. |
| Header Group (CC) | pattern | Built (visual only) | [node 4105:4238](https://www.figma.com/design/ETKqleZdpertwFEo40YB5n/Affino-CC-Hybrid--Design-System?node-id=4105-4238) | `src/cc/patterns/HeaderGroup/` — Compiled assembly stacking CCTopNavigation above CCHeader. **7 variants** (Device × Type): Default / Zone Selector / User Menu × Desktop / Mobile (6, 2026-05-27) + **Type=IconNavigation** (Desktop only, `4219:6346`, 2026-06-03). Zone Selector and User Menu are showcase types — they overlay a Dropdown in its open state. **IconNavigation** inserts the IconNavigation strip between TopNavigation and Header — hidden by default, shown via the User Menu "Icon Navigation" toggle (`HeaderGroup.js` → `.cc-header-group--icon-nav`); the revealed "Hide Labels" sub-toggle adds `.cc-icon-nav--no-labels`. Strip keeps its `--cc-actions-menu-secondary-bg` override (vs Figma `--ai-surface-minimal`, user decision). ⚠ **Some Figma variant labels are swapped vs widths**; width treated as source of truth. Composes CCTopNavigation, CCHeader, **IconNavigation**, Dropdown (transitively pulls in Breadcrumb, Button, NotificationBadge, Portraits, DropdownItem, ThemeToggle, Toggle). |
| Actions Menu (CC) | pattern | Built (visual only) | [node 4147:6727](https://www.figma.com/design/ETKqleZdpertwFEo40YB5n/Affino-CC-Hybrid--Design-System?node-id=4147-6727) | `src/cc/patterns/ActionsMenu/` — Vertical 56px right-side icon-only action bar. Stacked 40px buttons (Sparkles / Info / Star / A-Large-Small / Minimize-2 / Printer in the Figma reference). New `--cc-actions-menu-*` token family (primary-bg / secondary-bg / icon) added 2026-05-27; light-grey theme distinct from the dark CC Sidebar. MenuBtn implemented inline as scoped CSS (Figma layer, not a Component instance). No JS — `:hover` and `.is-hover` produce the highlight state. |
| AiAssistant (CC) | template | Built | [node 4152:8323](https://www.figma.com/design/ETKqleZdpertwFEo40YB5n/Affino-CC-Hybrid--Design-System?node-id=4152-8323) | `src/cc/templates/AiAssistant/` — CC-themed floating AI chat widget (320px / `--ai-size-6`). Sibling of `AiChatMinimised` (384px) — same drag + top-edge vertical resize + internal sidebar overlay, but with `data-brand="cc"`, a simpler header (plain "Affino Assistant" title, no selector dropdown), and a custom Initial screen (verbatim 3-paragraph welcome from Figma instead of the SuggestedQuestion grid). Processing + Response reuse the standard ChatMain markup. Maximise button is visual-only pending the full-page CC AI Chat template. |
| ControlScreen (CC) | template | Built | [node 4187:21254](https://www.figma.com/design/ETKqleZdpertwFEo40YB5n/Affino-CC-Hybrid--Design-System?node-id=4187-21254) | `src/cc/templates/ControlScreen/` — CC home/landing dashboard. App-shell layout (fixed sidebar + scrolling main col) composing existing patterns verbatim: `cc-sidebar-menu` (Desktop + Mobile composites — sidebar swap on viewport @media), `cc-header-group` (with `cc-header--control` greeting + the **IconNavigation** strip wired to the User Menu "Icon Navigation"/"Hide Labels" toggles via `HeaderGroup.js`; hidden by default, hidden <768 of the group width), Alert (Warning CTA), StatCard ×2 (Key Features / Help Guides), UpgradeCard (Version + Update), Select (Select Zone), Chart ×2 (Page Views / Users), Datatables (base Whos Online, no `--mobile-scroll`). Variants: Type=Menu Open/Collapsed × Device=Desktop/Mobile (4 in Figma) — Menu Open/Collapsed handled by existing `sidebar-menu.js`; no new JS. Template-level layout reflow uses **`@container cs-page (max-width: 767px)`** on `.cc-control__page` so the page reflows when the main column is narrow (docked sidebar shrinks it below 768 on a wide screen). Sidebar device-class swap stays on `@media`. Two copies of the Analysis Dashboard link in the DOM (`--desktop` inside zone-row, `--mobile` after charts) — CSS swaps which is visible per the Figma mobile layout. **CQ refactor complete (2026-06):** the composed Alert, Datatables, and cc-header all reflow via `@container` on their own width (not viewport), so they go mobile in a narrow-column-on-wide-viewport scenario. cc-header is wrapped in `.cc-header-cq` (its own container; an element can't query itself). **Backend-pending:** front-end complete but all data is hardcoded and most actions are mock — see `HANDOVER.md` + `docs/handover-manifest.json` (`TODO(backend:ControlScreen)` markers in the file). |
| FontSizeSlider (CC) | component | Built | [node 4146:6685](https://www.figma.com/design/ETKqleZdpertwFEo40YB5n/Affino-CC-Hybrid--Design-System?node-id=4146-6685) | `src/cc/components/FontSizeSlider/` — Small 4-step (S/M/L/XL) range slider intended for CC ActionsMenu contexts. Wrapper bg `--cc-actions-menu-primary-bg`; track unfilled + thumb border use `--cc-actions-menu-secondary-bg`; filled track + thumb fill use `--ai-surface-brand`. Standalone build (not a wrapper around `.rs`) — markup is `<input type="range" min="1" max="4">` + styled native `::-webkit-slider-thumb` / `::-moz-range-thumb`; JS paints the Webkit fill gradient via background-image. UI only — not yet wired to a font-scale CSS variable. |
| IconNavigation (CC) | pattern | Built | [node 4218:4681](https://www.figma.com/design/ETKqleZdpertwFEo40YB5n/Affino-CC-Hybrid--Design-System?node-id=4218-4681) | `src/cc/patterns/IconNavigation/` — Full-width strip on `--ai-surface-minimal` with nine module shortcuts (Publish / Promote / Social / Media / Structure / Design / Commerce / Analyse / Settings) distributed across a 768-wide (`--ai-size-11`) centred row. Single Figma variant (Tier=Pattern). Each item is an `<a>` (per user) with a CSS hover darkening glyph + label `--ai-text-secondary` → `--ai-text-primary`; no active/selected state (Figma defines none). Glyphs are the **exact Figma SVGs** in `img/cc-nav/*.svg`, painted via CSS `mask` in `--ai-text-secondary` (same brand-asset convention as `affinoMark`/`affinoLogo`) so they re-paint for CC dark mode. Glyph dimensions are brand-asset intrinsic px (allowed exception). Demo activates `data-brand="cc"`. No JS. |
| ControlHub (CC) | template | Built (visual only) | [node 4244:6526](https://www.figma.com/design/ETKqleZdpertwFEo40YB5n/Affino-CC-Hybrid--Design-System?node-id=4244-6526) | `src/cc/templates/ControlHub/` — CC filterable directory screen (a second "Control Screen" view). **Reuses the ControlScreen app-shell verbatim** (`../ControlScreen/ControlScreen.css` + cloned shell markup: rail + chrome + sidebar composites + ActionsMenu rail + AiAssistant/coachmark/toasts). New page content only: a filter **Input** (search + clear) + two sections (Analysis, Ad Campaigns), each a `repeat(3,1fr)`→`1fr` grid (gap `--ai-spacing-4`) of **ActionCard**s (Chevron + "add" Button-Tertiary-xs), Figma content verbatim. Composes Input, ActionCard (→ Button xs). Desktop `4236:6169` / Mobile `4243:6400`; reflow via `@container cs-page`. **Visual only:** filter + card actions are mock — `TODO(backend:ControlHub)` markers + `docs/handover-manifest.json`. Filter Input uses the component's `--ai-spacing-4` padding verbatim (Figma instance shows `--ai-spacing-5`; user-approved cross-file diff). |
| Seating Planner (CC) | template | Built | [desktop 3515:175956](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=3515-175956) · [mobile 3515:213358](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=3515-213358) | `src/cc/templates/SeatingPlanner/` — the Seating Planner screen inside the CC app shell. **Screen 1 of a series** ("No Event" empty state); the designer supplies a desktop + mobile frame per screen, so `.seating-screen` is the content card and `.seating-screen__empty` is one state inside it — later screens add siblings rather than reworking the card. **Reuses the ControlScreen app-shell verbatim** (`../ControlScreen/ControlScreen.css`), the same approach ControlHub takes; this file adds only page-content classes. Figma *confirms* the reuse: the sidebar's six glyphs are ControlScreen's exact set, and the mobile frame drops the ActionsMenu and swaps the rail precisely as the shell's own `@media (max-width: 768px)` rules already do. **Three shell CONFIGURATION fixes, no CSS** — caught by measuring, not by reading the markup: the sidebar menu panel was docked open (rail rendered **336px** vs Figma's collapsed 56), the chrome carried ControlScreen's `cc-header` title row (**131px** vs Figma's 48 — removed, not hidden, so nothing reserves height), and the breadcrumb was relabelled to Affino.com › Events › Seating Planner. **Step 3a paint bindings recorded before any CSS**: the page bg needed nothing because Figma binds `--cc-ui-primary-bg`, which `.cc-control` already paints — the very token ControlScreen originally got wrong by intuition. One departure from the shell: `.cc-control__page` gives `--ai-spacing-6` (24) but Figma binds `--ai-spacing-7` (32), 12 on mobile → the `--seating` modifier. Two designer rebinds 2026-08-26, both 'a token that works → the token that means it': Container bg `--ai-datatable-table-bg` → `--ai-surface-primary` (the same Datatables-token slip found on SeatingToast) and the disc glyph `--ai-surface-brand` → `--ai-icon-brand` (a rebind, not a gap — unlike SeatingToast's success/error icons, `--ai-icon-brand` exists with the identical value). Figma's raw `px-[33px] py-[65px]` decode to `--ai-spacing-7`/`--ai-spacing-11` **plus the 1px stroke**, and `size-[48px]` is `--ai-spacing-9` — nothing off-scale. `scrollbar-gutter: auto` releases the shell's stable 15px gutter, since this page never scrolls (its single child fills it and owns its own scrolling) — exactly the 15px that left the card at 1537 instead of 1552. Responsive via `@media`, not `@container cs-page`: the page padding **cannot** be a container query (an element can't query the container it establishes), and splitting would put two switch points on one change. **Flagged:** both frames are named "No Event" and their content frame still carries the working title "TASK-344753 — SeatingPlanner — mobile toolbar bottom bar — v4"; Select Event is a **detached frame** in Figma, not a Button instance, though it matches `btn btn--secondary` base on every property (its `shadow-xxs` is left as a Button-level question); Figma shows **no page header** at all; the empty-state icon frame is named "Text". Verified: sidebar 56, ActionsMenu 56, main 1616 and card **1552 wide all exact**; **zero JS errors** with the shell's whole script bundle ported unchanged. The mobile chrome renders 41 vs Figma's 48 **deliberately** — TopNavigation's own `@container (max-width: 767px)` sets 40px, and a template must not override a component's own responsive spec; Figma's instance was likely just not switched to its narrow state. |
| Login (CC) | template | Built (visual only) | [node 4254:11692](https://www.figma.com/design/ETKqleZdpertwFEo40YB5n/Affino-CC-Hybrid--Design-System?node-id=4254-11692) | `src/cc/templates/Login/` — CC Control Centre login screen. Centred card composing **Input** ×2 (email `mail` / password `lock`), **Checkbox** (Remember me) and **Button** (`btn--primary`, full-width Login), under the Affino wordmark + "Control Centre Login" title, with Forgot-password / Register-Now links. Renders under `data-brand="cc"` (brand teal `#3391a4`). Shell paint is Figma-bound: page bg `--cc-ui-primary-bg`, card bg `--cc-ui-secondary-bg`, `--ai-shadow-sm`, column max `--ai-size-9`. **Scoped override:** Input fields + checkbox box use `--ai-surface-input` (not the default `surface-primary`). Wordmark via CSS-mask of `img/affinoLogo.svg` painted `--ai-surface-brand` (the `.cc-menu__brand` pattern). Device axis (Desktop `4254:11691` / Mobile `4254:11690`) handled responsively at `@media (max-width:767px)` — mobile drops paddings/gaps/title size per Figma. **Visual only:** form submit + forgot/register links are mock — `TODO(backend:Login)` markers + `docs/handover-manifest.json`. |
| Event Picker (CC) | pattern | Built | [node 3108:6662](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=3108-6662) | `src/cc/patterns/EventPicker/` — Seating Planner event chooser (Control > CRM). **8 variants**: Device (Desktop/Mobile via `@media (max-width:767px)`) × Type (Event Picker / search / no results / all statuses). The four Types are *runtime states* driven by `event-picker.js` (predictive search + Live checkbox), not separate markup. Composes Modal, SearchInput, Checkbox, ActionCard, Badge, Button — Figma had no nested instances (built from prototype captures + tokenise plugin), but every value matched the real components. Case B overrides scoped to `.event-picker`: Modal gains a `--ai-border-secondary` hairline + `--ai-shadow-md`, footer → `space-between`, ActionCard gap → `--ai-spacing-5`. **Deliberate deviation:** the pinned (last-used) card carries no brand tint/border — Figma draws one, removed 2026-07-28 by the designer; the row is identified by its "Last used" group heading and top position (`--pinned` retained as an unstyled markup hook). **`--ai-shadow-md` was corrected** to Figma's two-layer `light/shadow-md` (affects all users of that token). Emits `event-picker:select` / `event-picker:close`; last-used persistence is the host's job (backend TODO). Figma set is authored in the **standard theme** (designer-confirmed) while the demo runs `data-brand="cc"` like the other CC demos — it previews the palette the pattern actually ships in. A **"CC brand" toolbar toggle** flips to the standard palette for Figma comparison. CSS is brand-agnostic (semantic tokens only). **Designer amends 2026-07-28** (no Figma counterpart — Figma draws only 768/382): thin `scrollbar-color` on the list; `.event-picker__stats` gains a `border-top` + `--ai-spacing-3` padding below `--ai-bp-md`; filters/list/empty padding → `--ai-spacing-5` below `--ai-bp-sm`. `--ai-shadow-md`'s contact layer was also softened to `0.05` light / `0.125` dark, now intentionally lighter than Figma's `light/shadow-md`. |
| AssistantPopover (CC) | pattern | Built (visual only) | [node 4218:5304](https://www.figma.com/design/ETKqleZdpertwFEo40YB5n/Affino-CC-Hybrid--Design-System?node-id=4218-5304) | `src/cc/patterns/AssistantPopover/` — Standalone pop-over/call-out (320px / `--ai-size-6`) that loads on the ControlScreen to highlight the Affino Assistant. **Built standalone, NOT part of the AiAssistant template** (Figma models it as AiAssistant `Type=Pop Over`, per user). Self-contained header (AI `sparkles` badge on `--cc-actions-menu-secondary-bg` + "Affino Assistant" + tertiary close ✕ + `--ai-border-secondary` divider), intro text, full-width `btn--primary` Launch with `arrow-right`. Card uses `--ai-shadow-md` + `--ai-radius-lg`. Composes Button. Intro paragraph gap 7px→`--ai-spacing-3`; intro font `--ai-font-fixed-xs` (user override of Figma's fluid-sm); close forced transparent bg (scoped, CC tertiary bg is grey). **Close-to-dismiss wired** (`AssistantPopover.js` removes the pop-over); positioning/show logic still to be specced. Demo activates `data-brand="cc"`. |
| Actions Info Panel (CC) | pattern | Built (visual only) | [node 4165:4007](https://www.figma.com/design/ETKqleZdpertwFEo40YB5n/Affino-CC-Hybrid--Design-System?node-id=4165-4007) | `src/cc/patterns/ActionsInfoPanel/` — White card pairing with the CC ActionsMenu. Three sections (Description / Related / System Security Right) each headed by `--ai-font-fixed-xs` semibold + 4px gap. Related + Security pills use the existing `btn btn--tertiary btn--sm` (text-only, no icons). Width pinned to `--ai-size-5` (280px — Figma was 288, no exact token); body line-height uses `--ai-leading-md` (Figma raw 1.5 rebound by approval). Composes Button. No JS — pill click handlers are a follow-up. |
| StatCard | pattern | Built | [frame 2758:3020](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2758-3020) | `src/patterns/StatCard/` — 10 variants: Size (Base/Lg) × Type (Default / Chevron Down / Chevron Right / No card / Number First). Brand icon square (Blue/600 primitive — user-approved gap, no `--ai-*` token) holds a Lucide icon, text column has label + value. `Number First` swaps order via CSS `order`; `No card` strips card chrome to transparent (diverges from Figma's white surface-primary, per user approval). Width is consumer-controlled (`width: 100%`). No JS — chevrons are static; consumer wraps in `<a>` / `<button>` if interaction needed. |
| UpgradeCard | pattern | Built | [frame 2758:3140](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2758-3140) | `src/patterns/UpgradeCard/` — 4 variants: Size (Base/Lg) × Type (Update/No updates). Version + status text column with optional green Update CTA. Update button is a `btn btn--primary btn--sm` with a **Case-B contextual override** scoped to `.upgrade-card .btn--primary` that swaps the bg to `--ai-surface-success` (the base Button stays primary-blue elsewhere). Width is consumer-controlled. Composes Button. |
| Alert | component | Built | [frame 2542:5858](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2542-5858) | `src/components/Alert/` — **23 variants**: Type (Info/Success/Danger/Warning/Neutral) × Style (Default/CTA/Floating/Fixed). CTA includes Mobile (stacks below 768px via `@media`); Floating only defined for Info/Success/Neutral per Figma. Each Type uses its semantic `--ai-{surface,text,border}-{type}` family; Danger maps to `error` namespace. CTA button is `btn btn--primary btn--sm` with a **Case-B contextual override** scoping bg to `--ai-surface-{type}` per Type (5 colour overrides). Composes Button. No JS. Replaces the prior `src/prototypes/Alerts/` Flowbite-inspired sketch (deleted 2026-05-29). |
| AutoAnswers (CC) | pattern | Built | [node 2802:6352](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2802-6352) | `src/cc/patterns/AutoAnswers/` — AI auto-answer card for support forums. **6 states × Device** (12 Figma variants): ProcessingAnswer (a `loader-circle` spinner replaces the header sparkles while loading — sparkles returns in loaded states; CSS animations: left→right shimmer on the info→cyan "thinking" title + the spinning loader, both halt under prefers-reduced-motion), SuggestedAnswer (info; "Awaiting Feedback" pill + 3 feedback buttons), NoAnswer (neutral; single paragraph), CompleteAnswer (success), PartialAnswer (warning), InadequateAnswer (neutral). State = `.cc-auto-answer--<state>` modifier on the root (query container); inner `.cc-auto-answer__card` holds visuals; `--cc-aa-{surface,border,accent}` drive the palette. Theme-agnostic `--ai-*` tokens (demo under `data-brand="cc"`). Device handled via **container query** `@container cc-auto-answer (max-width:767px)` — card adapts to its container width, not the viewport: below MD the header stacks to a column (pill below title), type shrinks, and action buttons drop to the sm size (h32/px12/12px) + stack. Demo wraps ALL variants in one resizable frame + width presets so the whole set transitions together. Action buttons are **local classes** (`__action--positive/caution/negative` = solid success / soft warning / soft error), not Button variants. JS: SuggestedAnswer's buttons resolve the card to a terminal marked state. Normalised Figma value drift (10px→radius-lg/md, 18px/26px→spacing-6, 15px→fixed-xs, pill rgba→`color-mix`); `#2dc0df` gradient stop user-approved. No nested DS-component dependencies (icons are Lucide). |

Add rows here as components are built. Format: Component Name, Tier (component/pattern/template), Built/In Progress/Figma Only, Figma URL, Notes.
| Filter Item | component | Built | [node 2972:1058](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2972-1058) | `src/components/FilterItem/` — Faceted filter chip. **12 variants**: State (Default / Empty / 1–4+ Selected) × Rounded (False=`--ai-radius-md` / True=`--ai-radius-full`). Default = solid `border-secondary` "+ Name"; Empty = dashed `border-contrast` placeholder; Selected = `surface-info-soft` fill + `border-info`, leading × clear + `Name · values` (medium · semibold) + chevron, count rolls up to "first, and N more" at 4+. All icons 12px = `--ai-icon-size-xs`. JS (`FilterItem.js`) auto-binds: trigger toggles `--open` (chevron flips) + emits `filter-item:toggle`; × resets to Default + emits `filter-item:clear`; `el.setFilterValues([…])` applies the rollup. **Visual + state API only** — no real value picker (`TODO(backend:Filters)`). No DS-component deps (icons Lucide; Button used in demo only). No token gaps. |
| Toast | component | Built | [node 2856:3020](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2856-3020) | `src/components/Toast/` — Transient notification card. **10 variants** on a single Figma "Type" enum, modelled as status × fill + two layout types: Info/Success/Danger/Warning (plain: white card + soft icon chip + status glyph + grey close) and their `…Color` filled siblings (`--color`: status-tinted card + status border/text/close, no chip bg); plus `--notification` (Avatar + name/message/`--ai-text-brand` time) and `--interactive` (title + body + Primary/Secondary `btn--sm` actions). Danger→`error` token family. Text uses `--ai-font-title`; shadow `--ai-shadow-md`; width `--ai-size-7` (384). Close is a scoped `.toast__close` icon button (Alert convention, not a Button instance); dismiss via `Toast.js` (document-delegated, mirrors Alert.js). Composes Avatar + Button. Notification 2px inter-line gap uses `--ai-spacing-0.5` (0.5 scale step added to Figma Tokens + re-exported for this build). Built from Figma after discarding the earlier `src/prototypes/Toasts/` sketch (deleted 2026-06-03). |
| Filter Bar | pattern | Built | [node 2977:3811](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=2977-3811) | `src/patterns/FilterBar/` — the filter toolbar. **Two-row card**: row 1 = saved-views + explicit **Export** `btn--secondary` (download icon) + persistent search field + kebab; row 2 = filter chips (rounded pills) + “Add Filters” last in the flow. JS modes (`FilterBar.js`): `--search` replaces row 1 only (chips row stays); `--new-view` keeps Export+actions and collapses row 2 to Add Filters; `--save-view` reveals a "Save view" CTA when a filter changes; `--alt-search` (mobile) shows a borderless views control + inline search. Mobile (`@media ≤767`): Export hidden, chips wrap, search collapses to icon + takeover. Composes Dropdown, FilterItem (rounded), Input, Button. Width consumer-controlled (Figma 970px frame, no token → fluid). `TODO(backend:Filters)`. |
| Table Type | component | Built | [node 3470:85494](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=3470-85494) | `src/components/TableType/` — Seating Planner table tier label. **5 variants**: Type (VIP/Head Table/Gold/Silver/Bronze) x Tier=Component. 18px pill, `--ai-radius-full`, uppercase `--ai-font-fixed-5xs`. **Tier colour is user-picked at runtime**, so one custom property `--table-type-color` drives all three paints via `color-mix()` — bg 80% white, border 70% white, text 25% black. Raw hex approved (picker values, not design tokens). 12 of 15 computed values exact vs Figma; Silver's text is the formula result `#80858A` where Figma has `#8B8D98` (documented divergence). Gold + Silver bases diverge from `--sp-table-gold` / `--sp-table-silver` — see figma-notes. No interactive states and **no dark-mode treatment** in Figma. No JS. |
| Attendee Card | component | Built | [node 3474:89292](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=3474-89292) | `src/components/AttendeeCard/` — Seating Planner attendee row (Unassigned tray, Table Detail). **12 variants**: Type (Attendee/VIP/Speaker/Sponsor/Host/Empty) x State (Default/Dragged Over) x Tier=Component. **Drop target only** — never drags; `--dragged-over` is toggled by the parent module. No JS. Composes Button (`btn--secondary btn--sm`) for Empty's Assign. Accent bar + role label share `--attendee-card-role`, bound to the `--sp-*` seating tokens (Radix Vivid default). Empty is structurally different: dashed border, neutral accent, no name/meta. Fluid width — Figma's 288px dropped, demo wraps at `--ai-size-5`. Seat badge `--ai-spacing-5` and action area `--ai-spacing-6` (Figma draws 18/28px, no token). First consumer of `--ai-shadow-xxs`. **a11y:** action hit areas 28/20px are under the 44px minimum — built as drawn, flagged. |
| Room Card | component | Built | [node 3470:84951](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=3470-84951) | `src/components/RoomCard/` — Seating Planner room/plan card. **Designer-confirmed as the componentised plan chip**, not a new entity above `SeatingPlan` — so no new backend table; name is the plan name and the counts derive from its tables/seats. The prototype's hand-built chip should be replaced by this component. **12 variants**: Type (Default/Seats Assigned/Full) x State (Default/Selected) x Device (Desktop/Mobile) x Tier=Component, collapsing to 4 CSS classes since Device is `@media (max-width: 767px)`. **Presentational only** — the parent owns selection and toggles `--selected`. **Type=Default and Type=Seats Assigned are the same CSS** (data-only difference), so only `--full` earns a modifier. The whole card is the select trigger: `.room-card__select` is a real `<button>` stretched via `::after`, with `.room-card__actions` raised on z-index so edit/delete stay clickable; focus rings the card via `:has()`. Progress bar is data-driven through `--room-card-progress` — Figma draws the Seats Assigned fill at 158px (~62%) which contradicts its own 124/148 (84%) label, so the drawn width is never ported. Composes Button via the **new `btn--icon.btn--2xs`** (24x24) added for this build — no Figma Button size produced it. Token work: `--ai-text-brand` replaces `--ai-surface-brand` on the seats-free text (3.60:1 -> 5.04:1, Figma updated); badge gap `border/width/border-3` -> `--ai-spacing-1`; `Grey/0` -> `--ai-text-invert`; **new token `--ai-font-fixed-6xs`** (9px) for the badge label; 7x7 check approved raw. **a11y:** 24x24 action buttons under the 44px minimum (built as drawn, flagged); FULL badge is white-on-success at 3.16:1 — a palette-level issue, flagged not resolved; progress bar `aria-hidden` since the counts state the value in text. |
| Full Badge | component | Built | [frame 3470:84969](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=3470-84969) | `src/components/FullBadge/` — "FULL" capacity pill. **No Figma component exists**: it is drawn as an inline `Full-Badge` FRAME in four places (RoomCard `3470:84969`/`3474:90697`, TableCard `3472:85703`/`3484:186684`), so it was **extracted from code** on 2026-08-25 rather than built from a set. Consequently **no `.figma.ts`** — Code Connect needs a component to attach to. No variants, sizes or modifiers, because Figma defines exactly one appearance. Consumed by RoomCard + TableCard. CSS leads Figma twice: check icon `10x10` + `stroke-width: 4` (Figma's 7x7 hairline was illegible — Lucide's 24-unit viewBox thins stroke-2 to ~0.6px), and `--ai-tracking-7` on the label. Renders 49.9x15 vs Figma's 45x15. Token fixes: badge gap `border/width/border-3` -> `--ai-spacing-1`; `Grey/0` -> `--ai-text-invert`; label 9px -> `--ai-font-fixed-6xs`. **a11y OPEN:** white on `--ai-surface-success` is 3.16:1 vs the 4.5:1 needed for 9px bold — a palette decision, deliberately parked. |
| Table Card | component | Built | [node 3470:85483](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=3470-85483) | `src/components/TableCard/` — Seating Planner table summary card (tables grid). **12 variants**: Type (Empty/Populated/Full) x State (Default/Selected) x Device (Desktop/Mobile) x Tier=Component, collapsing to **one CSS class plus a media query** — Device is `@media (max-width: 767px)` and **the whole Type axis is data, not CSS** (segment count, legend rows, badge presence), so there are no Type modifiers. **Presentational only** — the parent owns selection and toggles `--selected`; selecting a table opens Table Detail. Whole card is the select trigger (stretched `::after`, actions raised on z-index, focus ringed via `:has()`). Composes **TableType**, **FullBadge** and Button (`btn--2xs`). **Capacity bar is proportional**: each segment carries its count in `--seg` and divides the track by `flex-grow` on a 0 basis — verified exact at 20/10/10/20/40%. Figma draws fixed widths (62/31/31/62) that put 6-of-10 at 73% of the track and contradict its own legend; not ported. Figma's Empty variant also reads `Empty (4)` beside `0 / 10 seated`, and Full keeps Populated's legend beside `10 / 10 seated`. Token work: `--ai-font-fixed-6xs` (a FONT SIZE token) was bound as two 9px gaps -> snapped to `--ai-spacing-3`; **new token `--ai-radius-xs`** (2px, from Scale) for the legend swatch; the `line` stroke resolved to `--ai-border-secondary` via `get_variable_defs` on the node. **Header restructured 2026-08-25**: `Header-Section` is now a column, the tier pill moved inside the title row with a 12px gap, and the sponsor row spans the full card width — so a long table name **wraps** (Figma dropped its `nowrap`) and a long sponsor name has room. This retired two earlier divergences (the 0 header gap and the title ellipsis). **Only 1 of 12 Figma variants carries the restructure** — `3470:85480`; the other eleven still have the old nested header. Three divergences remain: ellipsis on the sponsor name (Figma pairs `nowrap` with `shrink-0` and would overflow), the legend at `--ai-font-medium` where Figma has regular, and the sponsor row's `padding-block-start` tightened to 4px on mobile only (Figma keeps 6px at both). **Figma issues flagged:** the three variants expose three different property sets, and on Default `Show Sponsor` also hides the tier pill; `--sp-host` is missing from the legend; bar layers are named `Bar-Staff`/`Bar-Guest` while the legend says Attendee/Sponsor. **a11y:** bar is `aria-hidden` (the legend states every count as text); 24x24 action buttons under the 44px minimum, built as drawn. **No dark mode** — `--sp-*` are not theme-aware. |
| Table Detail | pattern | Built | [node 3475:94010](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=3475-94010) | `src/patterns/TableDetail/` — Seating Planner table inspector: a fixed 320px rail (`--ai-size-6`) listing every seat. **8 variants**: Type (Default/2 Roles/All Roles/Full) x Tier=Pattern x Device, collapsing to **one CSS class plus a media query** — Device is `@media (max-width: 767px)` and **the whole Type axis is data, not CSS** (Type names the count of distinct roles seated; the legend is DERIVED, never stored, which is why Default has none at all). Composes **AttendeeCard** (one per seat), **TableType**, and Button via AttendeeCard's Assign. Presentational, no JS — seat rows are drop targets the parent toggles. Seat list has a **transparent scroll track with a thin `--ai-surface-secondary` thumb** (matching `.chat-sidebar__sections`); note Chrome honours `scrollbar-width: thin` at 11px and ignores the `::-webkit-scrollbar` width, so that 6px is legacy-only. **Height: fills its column, seat list scrolls** inside a fixed header (`block-size: 100%` + `flex: 1`/`min-block-size: 0`/`overflow-y: auto`; panel `overflow: hidden` clips the scroll inside the radius). Figma's `h-[698px]` was dropped — it is just ten seats' worth of height with no token. **Mobile drops the panel header entirely** (all four variants), plus padding 16→8px and seat gap 8→6px. Token work: `--ai-radius-xs` on the legend swatch; the handshake colour resolved via `get_variable_defs` on the icon node. **CSS leads Figma on the legend ROW gap only** — `--ai-spacing-0-5` (2px) against Figma's 4px; the 8px column gap is Figma's, unchanged. That matters because the five-role legend needs 269px against 257px available and so **wraps to two rows** where Figma fits one — a 12px shortfall, flagged in the notes. **Flagged:** the sponsor row's `h-[22px]` is off-scale and unbound here (unlike TableCard's derivable 22px), so it is left to content — header renders 73px vs Figma's 80px; `panel-header` carries a zero-width border with a colour still set; the handshake icon is `--ai-icon-secondary` here but `--ai-icon-contrast` in TableCard, and the sponsor font-family `--ai-font-body` vs TableCard's `--ai-font-title`; AttendeeCard's seat badge has drifted to 9px in Figma (component stays source of truth). **a11y:** hiding the mobile header removes the table name and count from the a11y tree — only correct because the mobile sheet supplies them; panel carries an `aria-label`. Legend is real text, not colour alone. **No dark mode** — `--sp-*` are not theme-aware. |
| Unassigned | pattern | Built | [node 3478:111942](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=3478-111942) | `src/patterns/Unassigned/` — Seating Planner tray of attendees not yet seated. Fixed 320px rail (`--ai-size-6`). **2 variants**: Type (Default = empty state / `Unassiged` [sic] = populated). **No Tier and no Device axis** — unique in this module; Tier=Pattern per §4 since it composes two components, and the mobile safety net was checked ("mobile" appears zero times in the design context or variables, so there is no mobile layout). **Type is data, not CSS** — `__empty` and `__list` are just different children of `__body`, so no Type modifiers. Composes **AttendeeCard with BOTH booleans off** (no seat number, no actions — an unseated person has neither) and **Input** (`input--sm`, label-less, leading search icon, `aria-label` since a placeholder is not an accessible name). Presentational, no JS — these rows are the drag SOURCES the seat rows receive. Token decisions: `w-[321px]` → `--ai-size-6` (320px, 1px Figma slip, matches TableDetail's rail); unbound `line-height: 1.4` → `--ai-leading-xs`; the empty-state tick keeps `--ai-surface-success` because **the icon scale has no success entry at all** (3.16:1 on white clears the 3:1 for a graphical object) — `--ai-icon-success` is a real hole worth filling. The search field spans the sheet via `inline-size: 100%`; it briefly needed a `max-inline-size: none` override too, until **Input's undocumented 192px `--sm` cap was removed on 2026-08-25**. **Flagged:** Figma's variant name is misspelled `Unassiged`; the PUBLISHED Code Connect for Input is stale (serves `input__field`, the class is `input__control` — a re-publish fixes it); the body slot is named `Body` on one variant and `guest-list` on the other; `Left-Footer-Area` is now reused for three unrelated things across the module. **Height grows with content** (Figma sets none) — unlike TableDetail which fills and scrolls, so the two side rails behave differently until someone decides. **No dark mode** — `--sp-*` are not theme-aware. |
| Table Listing | pattern | Built | [node 3488:196339](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=3488-196339) | `src/patterns/TableListing/` — Seating Planner tables grid plus its toolbar. **2 variants**: Tier=Pattern × Device (Desktop/Mobile). Fluid width — Figma's 1180/390 are unbound frames, and this is the main content area, not a rail. Composes **TableCard** (sponsor row and tier pill both `hidden` on the instances), **Toggle** (`toggle--xxs`) and **Input** (`input--sm`, label-less, `--ai-size-6` wide). Presentational, no JS — filtering and the free-seats toggle are the parent's job. **Grid is `repeat(auto-fill, minmax(--ai-size-4, 1fr))`** rather than Figma's literal 4 columns: TableCard's 240px min-width means 4 fixed columns need ~984px and would overflow between 768–1000px. Measured to render **4 columns of 281px at Figma's 1148px grid width — exactly the cards Figma draws** — and 1 at mobile, stepping through 3 and 2 in between; the mobile single column needs no rule. Toolbar nests title+filter in `__toolbar-main` to reproduce Figma's equal flex-1 split (396+12+396 = 804, +12+320 = 1148) while giving the mobile two-row layout for free. Mobile: toolbar → column, divider hidden, title 18→16px, root padding-inline 16→12px, search full width. **Two child components changed (both Case A):** Toggle gained the `xxs` size it was missing, and **Input's undocumented 192px `--sm` cap was removed** after proving by measurement that it shaped no existing consumer. **Flagged:** a hidden `View Actions` frame (2 Buttons, likely a view switch) is not built; the filter label's 12px is unbound in Figma; the published Code Connect for Input is stale (`input__field` vs `input__control`); the registry cites node `2025:1080` for both Toggle and ToggleDS, which is Toggle's set. |
| Seating Header | pattern | Built | [node 3474:90519](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=3474-90519) | `src/patterns/SeatingHeader/` — Seating Planner event bar + room selector + toolbar. **Filed as SeatingHeader because Figma names it `Header` and `src/patterns/Header` is the unrelated AI-Chat header** — worth renaming in Figma. **4 variants**: Tier=Pattern × Type (Has Plans/No Plans) × Device, collapsing to one CSS class plus a media query. **Type is data, not CSS** — `No Plans` is the event bar alone (verified: `3474:90517` contains only `Event-Info-Bar`, byte-identical to Has Plans'), so no Type modifiers; the fifth component in this module where that holds. Composes **RoomCard** (one per plan), **Button** (base / `--sm` / `--icon --xs`) and **Toggle** (`toggle--xs`, label-less) — `toggle--xs` matched Figma's 32×16 track and 12px knob exactly, so **no component change was needed this time**. Fluid: Figma's `w-[1552px]` root and `w-[900px]` details block are both unbound and match no token, so root is `100%` and details `flex: 1 0 0` (designer, 2026-08-26) — same call as AttendeeCard/TableCard/TableListing. Desktop room cards pinned to `--ai-size-5` (280px) against Figma's untokenised 290 — **the identical call RoomCard itself already took**, so the two agree. **The room selector is a scrollbar-less carousel**: Figma's own mobile variant overflows its frame (512px of cards in 392px, second card visibly clipped), so a scroller is the faithful reading — and hiding the bar is *closer* to Figma, which draws no scrollbar anywhere (it also brought the mobile height from 335 to 320 against Figma's 317). Deliberately NOT TableDetail's thin-bar treatment: that is a long vertical list where the bar is a position indicator, this is a carousel. Touch swipes natively (not reimplemented), keyboard works via the cards' own buttons pulling focus into view, and **`SeatingHeader.js` adds mouse drag** — `is-scrollable`/`cursor: grab` only when it actually overflows, a 5px threshold so a drag ending on a card's Delete does not fire it (click swallowed in the capture phase, flag cleared on next pointerdown rather than a timer, since a drag released off-window fires no click), no `preventDefault` on pointerdown so the card buttons keep focus, and touch pointers ignored outright. **The toolbar has its own breakpoint — it stays compact to 1023px** while everything else reverts at 767px, because it carries name + count + switch + divider + 3 buttons + kebab and runs out of room first; 1024 matches the Seating Planner shell's own boundary (`SeatingPlannerShell.css` uses `max-width: 1023px`). That makes **three** responsive bands, the middle one being a state no Figma variant shows — previewed at 900px in the demo. The toolbar's *padding* deliberately stays at 767 so all three regions keep a common inline padding (verified 24px at 800/900). `.seating-header__bar` carries `gap: --ai-spacing-7` (designer) — Figma needs none against a fixed 900px block, but with the block fluid the gap is what keeps a long title off the buttons. **The kebab is a bare 20px icon layer in Figma, not a Button** — built as a real `<button>` with a stretched 44×44 `::after` (designer, 2026-08-26), since a bare `<i>` is not focusable. Toolbar reproduces TableListing's equal flex-1 split. `Toggle.js` supplies the switch behaviour; everything else is presentational. **Mobile changes content, not just styling**: `Room Layout` is hidden, `Add Table` shortens to `Add` and `Export` becomes icon-only — the dropped words are `clip-path`-hidden (not `display:none`) so labels stay in the a11y tree; global buttons step base→`sm`, and **Figma reverses their order so the primary `New Plan` leads** (built as drawn, flagged as possibly a layer-order slip). Method note: the title's `text-[22px]` looks unbound in design context but `--ai-font-fixed-xl` IS in the variant's variable list — **design context under-reports bindings, so cross-check `get_variable_defs` before raising a token gap**. **Flagged:** all three meta icons are named just `Icon` (identities read off a screenshot — `calendar`/`users`/`map-pin`); Figma's `Icon/24px/Export` has no Lucide name, built as `download` per FilterBar's precedent; the room-label `gap-[8px]` is unbound (`--ai-spacing-3` is exactly 8px); Code Connect omits the size class on the 32×24 swap button and the base-size global buttons; a single-child `Item` wrapper carrying a dead gap was dropped; the desktop title pairs `shrink-0` with ellipsis rules (inert); `Frame 248`/`Frame 252` are default layer names. **a11y:** kebab and swap button are real `<button>`s with `aria-label`s, `__meta` is a real `<ul>`, toggle wired via `label[for]` + `aria-labelledby`. **No dark mode.**
| Seating Toast | component | Built | [node 3474:90240](https://www.figma.com/design/Lus07xi8pPXLN87sQIyrEt/Affino-AI---Design-System?node-id=3474-90240) | `src/components/SeatingToast/` — Seating Planner action confirmation / warning **pill** with an Undo. **NOT the `Toast` component** — Figma names both just "Toast", but that one is the 384px notification CARD (`--ai-radius-lg`, 32px icon chip, close button, 10 variants) while this is a 465px pill (`--ai-radius-full`, bare 20px glyph, status border on an always-white ground, optional Undo, no close, no JS). Not reachable from Toast's axes, so a separate component; **renaming it `Seating Toast` in Figma is the top follow-up.** **2 variants**: Type (Success/Error) × Tier=Component. **No Device axis** — `max-inline-size: 100%` is what makes it safe on a narrow screen (verified 294px inside a 320px rail). Type changes only the border colour and the icon colour; the ground stays white in both, which is what separates it from Toast's tinted `--color` variants. **`Show Cta` is a real Figma boolean** but neither drawn variant turns it off, so the CTA-less height (46px) is emergent — flagged. Composes Button `btn--secondary btn--xs` with a **Case-B contextual override** on padding (`--ai-spacing-3 --ai-spacing-4`): the instance is 32 tall where `.btn--xs` is 28, and **Button's own set was checked (`53:2489`) rather than guessed — every `Size=xs` text variant there is 115×28**, so `.btn--xs` is correct and the override is scoped here, sparing ActionCard's 2 and ControlHub's 6 xs buttons. Designer calls 2026-08-26: unbound two-layer shadow (layer 1 is exactly `--ai-shadow-card`, layer 2 matches nothing) → **`--ai-shadow-md`**, the token the Toast card uses, for cross-component consistency over a literal match; `bg` rebound from **`--ai-datatable-table-bg`** (a Datatables token on a toast!) → `--ai-surface-primary`; `w-[465px]` → `--ai-size-8` (448) + `max-inline-size: 100%`. Figma's `px-[17px] py-[13px]` decode to `--ai-spacing-5`/`--ai-spacing-4` **plus the 1px stroke** — the arithmetic confirms it (1+12+32+12+1 = 58, Figma's stated height), so nothing is off-scale. Four more unbound values each match a token exactly and were bound: radius 100 → `--ai-radius-full`, gap 12 → `--ai-spacing-4`, message 14/20 → `--ai-font-fixed-xs`/`--ai-leading-sm`. Icon keeps `--ai-surface-success`/`-error` as drawn because **the icon scale has no success or error entry at all** — same hole flagged on Unassigned. Message wraps (Figma's `whitespace-nowrap` only holds for its sample strings); `<strong>` carries Figma's semibold emphasis so it is semantic, not visual. **a11y:** `role="status"` on Success, `role="alert"` on Error, matching the convention Toast already set; icon `aria-hidden`. **Flagged:** Figma's text layer names are stale (layer says "Placing Dame Helen Verity…", content says "Helen Verity successfully assigned to Seat 2"); message font-family is `--ai-font-body` here vs `--ai-font-title` on the Toast card; Code Connect omits the CTA's size class; **a pre-existing 2px in `btn--xs`** (Figma 28, ours 30) left untouched as a Button-level decision. Renders 448×60 vs Figma's 465×58 — width by decision, +2px from the CTA's own border. **No dark mode.**
